
<div class="col-md-8 col-md-offset-2 reviews-box">

<h1>Create a Gem</h1>

<%= simple_form_for @mygem do |f| %>

    <%= select_tag(:c_id, options_for_select(@all_categories), :prompt => "Select a category") %>
    <br><br>
    <%= f.input :title, label:"Gem Name" %>
    <%= f.input :description, label:"Gem Description" %>
    <br>
    <%= f.button :submit, "Submit", class: "btn btn-primary" %>

<% end %>

</div>

